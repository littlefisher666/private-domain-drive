## 1. 传输基础设施

- [ ] 1.1 将传输任务模型扩展为可表达真实字节进度、目标目录、批次关联、取消令牌和失败原因的状态。
- [ ] 1.2 实现上传下载共用的内存任务队列，支持排队、运行、取消、单项重试和 1 至 5 的总并发调度。
- [ ] 1.3 实现并持久化总并发数设置，默认值为 3；设置变更仅影响后续调度，不中断已运行任务。
- [ ] 1.4 将 OSS 下载改为流式接收和临时文件写入，在完成、失败和取消时正确处理目标文件与临时文件。
- [ ] 1.5 将现有上传和单文件下载入口接入真实任务队列，移除模拟进度逻辑。

## 2. OSS 批量操作

- [ ] 2.1 为对象列举补充分页能力，并实现目录递归展开和对象路径去重。
- [ ] 2.2 实现符合 OSS 限制的多对象删除分批请求及每批结果解析。
- [ ] 2.3 在控制器中编排批量删除、部分失败结果、目录缓存更新和当前路径回退。
- [ ] 2.4 在启动每个批量任务前检查并刷新 STS 凭证，确保所有对象路径受会话根目录约束。

## 3. 文件工作区多选与批量操作

- [ ] 3.1 增加独立于桌面详情单选状态的多选状态、范围选择和全选/取消选择行为。
- [ ] 3.2 在 macOS 列表和缩略图中实现选择入口、复选框、Cmd 切换、Shift 范围选择及 Cmd+A。
- [ ] 3.3 在 Android 列表和缩略图中实现长按进入多选、点击切换、全选和退出多选。
- [ ] 3.4 实现批量下载入口：一次选择目标目录、跳过文件夹提示、同名自动改名，并建立下载批次。
- [ ] 3.5 实现批量删除确认：展示不可恢复提示、文件夹数和展开后的对象数，并展示成功/失败汇总与重试入口。
- [ ] 3.6 确保操作完成或取消后刷新文件列表、清理选择状态，并维持现有权限控制。

## 4. 传输中心体验

- [ ] 4.1 在传输中心展示真实单文件进度、排队/取消/失败/重试状态和实际保存位置。
- [ ] 4.2 增加批量下载汇总展示，包括总数、进行中、完成、失败和排队数量，以及批次取消操作。
- [ ] 4.3 增加传输中心的并发数设置入口与当前运行/排队数量展示。

## 5. 验证与文档

- [ ] 5.1 为队列调度、动态调整并发、取消、重试、同名改名和失败清理添加单元测试。
- [ ] 5.2 为多选、批量下载跳过文件夹、批量删除确认和部分失败反馈添加组件或集成测试。
- [ ] 5.3 在 macOS 与 Android 上验证真实 OSS 下载进度、目标目录写入、权限受限和大目录删除路径。
- [ ] 5.4 更新 PRD、功能需求、用户故事、技术文档、Flutter 架构设计和一期检查项中的批量操作与并发配置说明。
