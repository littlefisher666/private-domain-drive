## 1. SDK 兼容性与统一契约

- [x] 1.1 根据阿里云官方文档核实并固定 OSS Android SDK 与 OSS SDK for Swift V2 的版本、依赖方式、最低 Android/macOS 版本及所需网络权限。
- [x] 1.2 在客户端建立包含 Dart facade、Android Kotlin 和 macOS Swift 实现的本地 Flutter OSS 插件骨架，注册 MethodChannel 与 EventChannel。
- [x] 1.3 定义并测试统一的会话配置、对象模型、操作参数、传输事件和错误码协议，确保平台类型与凭证秘密不会越过约定边界。

## 2. Android 官方 OSS SDK 适配

- [x] 2.1 引入阿里云 OSS Android SDK，实现 STS/Endpoint/Bucket 配置更新、客户端重建和退出登录清理。
- [x] 2.2 实现 Android 对象与目录列举、分页列举、目录占位对象创建、单删、批删和对象复制，并转换为统一结果模型。
- [x] 2.3 实现 Android 基于本地路径的普通/分片上传、下载到文件、SDK进度事件、请求句柄管理和真实取消。
- [x] 2.4 实现 Android 受限对象内容和 OSS 图片处理缩略图读取，保证大文件不通过平台通道返回。

## 3. macOS 官方 OSS SDK 适配

- [x] 3.1 引入阿里云 OSS SDK for Swift V2，实现 STS/Endpoint/Bucket 配置更新、客户端重建和退出登录清理。
- [x] 3.2 实现 macOS 对象与目录列举、分页列举、目录占位对象创建、单删、批删和对象复制，并转换为统一结果模型。
- [x] 3.3 实现 macOS 基于本地路径的普通/分片上传、下载到文件、SDK进度事件、请求句柄管理和真实取消。
- [x] 3.4 实现 macOS 受限对象内容和 OSS 图片处理缩略图读取，并验证文件选择后的沙盒访问在排队传输期间有效。
- [x] 3.5 增加 macOS 适配层测试，覆盖 STS 配置、模型转换、对象操作、进度、取消、错误映射和敏感日志脱敏。

## 4. Flutter 调用链与传输中心迁移

- [x] 4.1 实现 Flutter 平台 OSS facade 和可替换测试接口，将登录、会话恢复、STS 刷新和退出登录接入原生凭证生命周期。
- [x] 4.2 将文件选择和上传任务改为使用本地源路径，关闭完整字节读取，并在任务中保留源路径、对象键和大小用于重试。
- [x] 4.3 将列表、目录创建、递归/分页列举、删除、批量删除、复制、下载、预览和缩略图全部切换到平台 OSS facade。
- [x] 4.4 接入原生传输事件，按任务单调更新实际字节，使用时间窗口计算平滑速度并对 UI 更新节流。
- [x] 4.5 在字节传输完成但 OSS 尚未确认时展示“正在确认”，仅在 SDK最终成功后标记完成，并让取消操作真实中止原生请求。
- [x] 4.6 保留下载临时文件的成功改名、失败/取消清理，以及缩略图缓存、对象版本失效和默认图标降级行为。
- [x] 4.7 更新 Flutter 单元与组件测试，覆盖路径型上传重试、双向真实进度、平滑速度、等待确认、取消、统一错误和全部对象操作 facade。

## 5. 删除旧 OSS 实现

- [x] 5.1 删除 Dart `OssClient` 中的 REST 请求、Header 签名、XML 解析、普通/分片上传、流式下载及全部对象协议实现，不保留运行时 fallback。
- [x] 5.2 删除当前 1MB 强制分片和基于 Dart HTTP 缓冲/分片确认计算进度速度的临时代码。
- [x] 5.3 清理不再使用的 OSS 签名测试、辅助类型和依赖；确认 `crypto` 无其他使用后从 `pubspec.yaml` 移除，`http` 仅服务非 OSS 接口。
- [x] 5.4 全仓检索并确认生产代码中不存在绕过平台 facade 的 OSS HTTP 请求、手写签名、XML 响应解析或大文件 `List<int>` 上传路径。

## 6. 文档与 macOS 验证

- [x] 6.1 更新 `docs/技术文档.md` 与 `docs/Flutter架构设计.md`，记录双端官方 SDK、平台桥接、文件路径边界、进度速度和取消语义。
- [x] 6.2 运行 Flutter 静态检查、Dart 测试和 macOS 构建/测试，修复所有迁移引入的问题。
- [x] 6.3 使用真实测试 OSS 在 macOS 验证列表、上传、下载、缩略图、复制、删除、批删、进度、速度、取消、STS 刷新和沙盒文件访问。
- [x] 6.4 完成 `oss-image-thumbnails` 尚未结束的 macOS 真实 OSS 与体验验收，确认迁移后缓存和失败降级要求保持成立。
