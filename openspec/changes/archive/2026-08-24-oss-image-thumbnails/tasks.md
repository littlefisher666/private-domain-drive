## 1. OSS 缩略图读取能力

- [x] 1.1 在 `OssClient` 中增加图片缩略图读取方法，复用现有 STS Header 签名并附加 OSS `x-oss-process` resize 参数。
- [x] 1.2 定义缩略图规格和缓存键所需的对象版本字段，确保同一路径对象更新后不会复用旧结果。
- [x] 1.3 验证 OSS 图片处理请求的查询参数、签名资源和错误响应处理，确保不影响现有列表、下载和上传请求。

## 2. 客户端缩略图展示与缓存

- [x] 2.1 将网格中的图片占位组件接入缩略图读取结果，非图片继续使用默认类型图标。
- [x] 2.2 增加进程内缩略图缓存和加载状态，避免网格滚动或重建重复请求同一对象。
- [x] 2.3 实现网络、鉴权、OSS 图片处理和图片解码失败时的默认图片图标降级，保证单项失败不影响目录布局。
- [x] 2.4 检查 macOS 下的图片解码、网格尺寸和滚动体验，必要时限制缩略图请求并发。

## 3. 测试与联调

- [x] 3.1 增加 `OssClient` 缩略图请求测试，覆盖图片处理参数、STS 鉴权请求和失败响应。
- [x] 3.2 增加缩略图组件测试，覆盖成功展示、缓存命中、非图片图标和失败降级场景。
- [x] 3.3 在 macOS 客户端使用真实测试 OSS 对象验证常见图片格式、对象更新后的缓存失效和无权限时的稳定回退。
- [x] 3.4 运行 Flutter 静态检查和相关测试，并更新一期检查项中“图片显示缩略图”的验收记录。

## 4. 图片详情页预览

- [x] 4.1 使用 OSS 图片处理结果展示真实图片预览，并在失败时提供重试入口。
- [x] 4.2 将预览页下载改为现有传输队列，避免受文本内存读取上限影响。
- [x] 4.3 增加图片详情页预览读取规格测试并运行静态检查。
