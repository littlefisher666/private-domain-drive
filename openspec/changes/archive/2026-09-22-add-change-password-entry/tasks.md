## 1. 客户端改密页模式化

- [x] 1.1 为 `ChangePasswordPage` 增加模式参数（默认强制模式，保持既有路由行为不变），主动模式成功后提示成功、更新本地会话 `mustResetPassword` 为 false 并返回设置页
- [x] 1.2 确认强制模式路径（登录后拦截、冷启动拦截、成功进入文件空间）行为无回归

## 2. 设置入口与导航

- [x] 2.1 在 `SettingsPage` 新增"修改密码"入口并注册主动模式改密页路由（Android 与 macOS 共用）
- [x] 2.2 验证主动改密页无返回绕过强制改密拦截的路径（仅完成后返回设置）

## 3. 测试与验证

- [x] 3.1 为主动模式补充测试：成功返回设置页、当前密码错误、本地校验（不一致/少于 8 位）拒绝
- [x] 3.2 运行既有改密与会话相关测试确认无回归
- [x] 3.3 双端联调验证：设置入口进入、主动改密成功、失败重试、强制改密流不受影响
